export enum AnalyticsTimePeriod {
  Today,
  ThisWeek,
  ThisMonth,
  Total,
}

export enum UserActivityStatus {
  Active = 0,
  Completed = 1,
  Suspended = 2,
}

export enum TrackedModule {
  AutoComplete = 0,
  Chat = 1,
  CodeDebug = 2,
  CodeGen = 3,
  ImageCaption = 4,
  ImageEdit = 5,
  ImageGen = 6,
  Learn = 7,
  Meeting = 8,
  Payment = 9,
  Resume = 10,
  Sentiment = 11,
  SpeechToText = 12,
  Summary = 13,
  TextToSpeech = 14,
  Translate = 15,
}
