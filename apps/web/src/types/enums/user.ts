export enum AnalyticsTimePeriod {
  Today,
  ThisWeek,
  ThisMonth,
  Total,
}

export enum UserActivityStatus {
  Active = 0,
  Completed = 1,
  Suspended = 2,
}

export enum TrackedModule {
  Chat = 1,
  CodeDebug = 2,
  CodeGen = 3,
  ImageEdit = 4,
  ImageGen = 5,
  Payment = 6,
  Translate = 7,
}
